# Pi-hole local DNS records — hosts-file format: "<IP> <hostname>".
# This stack uses NO local hostnames (apps are reached by IP:port over HTTPS), so this
# is normally empty. Add lines here only if you want custom names, then:
#   docker exec pihole pihole reloaddns
